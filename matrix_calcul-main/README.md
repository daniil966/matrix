Калькулятор матриц Кузьмин Даниил ИС-323
📌 Описание
Проект представляет собой консольное приложение на языке Go, реализующее базовые операции с матрицами 2×2 и 3×3.
Программа написана с использованием модульной структуры, где каждая операция вынесена в отдельную функцию.
Цель: реализовать сложение, умножение на число и умножение двух матриц с пользовательским интерфейсом в консоли.

🧩 Структура проекта
matrix-calculator/
├── main.go # Основной файл с меню и логикой ввода/вывода
├── operations.go # Функции для математических операций
├── matrix.go # (опционально) Структура Matrix и методы
├── go.mod # Модуль Go
└── README.md # Документация

🧾 Описание кода (анализ по строкам)

🔹 main.go

go
1
Первые строки кода импортируют главный пакет main. Это точка входа в программу.

go
1234
import (
	"fmt"
	"os"
)
Импорт пакетов:

fmt — для ввода и вывода в консоль.
os — для завершения программы.
go
123
func main() {
	fmt.Println("=== Калькулятор матриц Кузьмин Даниил ИС-323 💾 ===")
	fmt.Println()
Через функцию main происходит вход в программу.
Сначала выводится заголовок и пустая строка.

go
123456
	fmt.Println("Выберите операцию:")
	for {
		fmt.Println("1. Сложение матриц")
		fmt.Println("2. Умножение матрицы на число")
		fmt.Println("3. Умножение двух матриц")
		fmt.Println("4. Выход")
Эти строки показывают пользователю список доступных операций с матрицами.

go
12
		var choice string
		fmt.Scanln(&choice)
Объявление переменной choice, которая хранит ввод пользователя (в виде строки), и её считывание.

go
12345678910111213
		switch choice {
		case "1":
			handleMatrixAddition()
		case "2":
			handleScalarMultiplication()
		case "3":
			handleMatrixMultiplication()
		case "4":
			fmt.Println("До свидания!")
			os.Exit(0)

Начало конструкции switch, которая обрабатывает выбранный вариант:

case "1" — вызывает функцию handleMatrixAddition() для сложения матриц.
case "2" — вызывает handleScalarMultiplication() для умножения матрицы на число.
case "3" — вызывает handleMatrixMultiplication() для умножения двух матриц.
case "4" — завершает программу.
default — выводит сообщение о некорректном выборе.
go
1234567891011121314
func handleMatrixAddition() {
	size := getMatrixSize()
	a := inputMatrix(size, "Первая матрица")
	b := inputMatrix(size, "Вторая матрица")

	result, err := AddMatrices(a, b)
	if err != nil {
		fmt.Printf("Ошибка: %s\n", err)
		return
	}

Функция для обработки сложения матриц:

Запрашивает размер матрицы.
Вводит обе матрицы.
Вызывает AddMatrices из operations.go.
Выводит результат или ошибку, если возникла.
go
1234567891011121314
func inputMatrix(size int, name string) [][]float64 {
	fmt.Printf("\n%s (%dx%d):\n", name, size, size)
	matrix := make([][]float64, size)
	for i := range matrix {
		matrix[i] = make([]float64, size)
		for j := range matrix[i] {
			fmt.Printf("Элемент [%d][%d]: ", i+1, j+1)
			var val float64
			fmt.Scanln(&val)
			matrix[i][j] = val

Функция inputMatrix:

Принимает size (размер матрицы) и name (имя матрицы).
Возвращает матрицу типа [][]float64.
Запрашивает у пользователя элементы матрицы по индексам [i][j].
go
123456789
func printMatrix(matrix [][]float64) {
	size := len(matrix)
	for i := 0; i < size; i++ {
		for j := 0; j < size; j++ {
			fmt.Printf("%8.2f ", matrix[i][j])
		}
		fmt.Println()
	}
}
Функция для вывода матрицы в консоль:

Двойной цикл проходит по строкам и столбцам.
Печатает значения с форматированием "%8.2f" (2 знака после запятой).
🔹 operations.go

go
123456789101112131415
func AddMatrices(a, b [][]float64) ([][]float64, error) {
	if len(a) != len(b) || len(a[0]) != len(b[0]) {
		return nil, errors.New("матрицы должны быть одинакового размера")
	}

	size := len(a)
	result := make([][]float64, size)
	for i := range result {
		result[i] = make([]float64, size)
		for j := range result[i] {

Функция для сложения двух матриц:

Проверяет, что размеры матриц совпадают.
Создаёт новую матрицу result.
Проходит по каждому элементу и складывает значения: a[i][j] + b[i][j].
Возвращает результат или ошибку.
go
1234567891011
func MultiplyMatrixByScalar(matrix [][]float64, scalar float64) [][]float64 {
	size := len(matrix)
	result := make([][]float64, size)
	for i := range result {
		result[i] = make([]float64, size)
		for j := range result[i] {
			result[i][j] = matrix[i][j] * scalar
		}
	}
	return result

Функция для умножения матрицы на число:

Создаёт новую матрицу result.
Проходит по каждому элементу и умножает на scalar.
go
1234567891011121314151617
Функция для умножения двух матриц:

Проверяет, что размеры совпадают.
Реализует классический алгоритм умножения:
result[i][j] += a[i][k] * b[k][j] — суммирует произведения соответствующих элементов.
Возвращает результат или ошибку.
📝 Вывод
Проект успешно реализует все требования ПР №3/5:
✅ Сложение матриц
✅ Умножение матрицы на число
✅ Умножение двух матриц
Код разделён на логические функции.
Поддерживает матрицы 2×2 и 3×3.
Удобный интерфейс в консоли.
Код соответствует требованиям ПР №5 — функционал разделён на функции.
