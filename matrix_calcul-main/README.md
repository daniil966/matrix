# Калькулятор матриц 🧮

**Автор:** Кузьмин Даниил ИС-323

## 🎯 Описание
Проект представляет собой консольное приложение на языке Go, реализующее базовые операции с матрицами 2×2 и 3×3. Программа написана с использованием модульной структуры, где каждая операция вынесена в отдельную функцию.

**Цель:** реализовать сложение, умножение на число и умножение двух матриц с пользовательским интерфейсом в консоли.

## 🚀 Быстрый старт

### Предварительные требования
- Установленный Go (версия 1.19 или выше)
- Git (для клонирования репозитория)

### Установка и запуск

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/matrix-calculator.git
cd matrix-calculator

# 2. Собрать проект
go build -o matrix-calculator main.go operations.go

# 3. Запустить приложение
./matrix-calculator

# Или запустить напрямую
go run main.go operations.go
Структура проекта
text
matrix-calculator/
├── main.go          # Основной файл с меню и логикой ввода/вывода
├── operations.go    # Функции для математических операций
├── matrix.go        # (опционально) Структура Matrix и методы
├── go.mod           # Модуль Go
├── go.sum           # Зависимости
└── README.md        # Документация
📖 Команды Git для проекта
Инициализация репозитория
bash
# Создание нового репозитория для матричного калькулятора
git init
echo "# Калькулятор матриц на Go" > README.md

# Создание базовой структуры файлов
touch main.go operations.go matrix.go go.mod

# Инициализация модуля Go
go mod init matrix-calculator

# Первый коммит
git add .
git commit -m "Initial commit: базовый калькулятор матриц"

# Настройка удаленного репозитория
git remote add origin https://github.com/yourusername/matrix-calculator.git
git branch -M main
git push -u origin main
Рабочий процесс разработки
bash
# Создание ветки для новой функциональности
git checkout -b feature/add-matrix-determinant

# Разработка новой функции
# ... редактирование operations.go ...

# Коммит изменений
git add operations.go
git commit -m "feat: добавить вычисление определителя матрицы"

# Возврат в основную ветку и слияние
git checkout main
git merge --no-ff feature/add-matrix-determinant -m "Merge feature: вычисление определителя"
git branch -d feature/add-matrix-determinant

# Отправка обновлений
git push origin main
Управление версиями и релизы
bash
# Создание тега для версии 1.0.0
git tag -a v1.0.0 -m "Версия 1.0.0: базовые операции с матрицами"
git push origin --tags

# Создание архива для сдачи проекта
git archive --format zip --output matrix-calculator-v1.0.0.zip main.go operations.go README.md

# Создание архива с полной историей
git archive --format zip --prefix=matrix-calculator/ -o matrix-calculator-full-v1.0.0.zip HEAD
🖥️ Использование программы
Главное меню
text
=== Калькулятор матриц Кузьмин Даниил ИС-323 🧮 ===

Выберите операцию:
1. Сложение матриц
2. Умножение матрицы на число
3. Умножение двух матриц
4. Выход

Ваш выбор:
Примеры работы
1. Сложение матриц
text
Ваш выбор: 1

Выберите размер матрицы (2 или 3): 2

Первая матрица (2x2):
Элемент [1][1]: 1
Элемент [1][2]: 2
Элемент [2][1]: 3
Элемент [2][2]: 4

Вторая матрица (2x2):
Элемент [1][1]: 5
Элемент [1][2]: 6
Элемент [2][1]: 7
Элемент [2][2]: 8

Результат сложения:
    6.00     8.00 
   10.00    12.00 
2. Умножение матрицы на число
text
Ваш выбор: 2

Выберите размер матрицы (2 или 3): 3

Матрица (3x3):
Элемент [1][1]: 1
Элемент [1][2]: 2
Элемент [1][3]: 3
Элемент [2][1]: 4
Элемент [2][2]: 5
Элемент [2][3]: 6
Элемент [3][1]: 7
Элемент [3][2]: 8
Элемент [3][3]: 9

Введите число для умножения: 2.5

Результат умножения на 2.50:
    2.50     5.00     7.50 
   10.00    12.50    15.00 
   17.50    20.00    22.50 
3. Умножение матриц
text
Ваш выбор: 3

Выберите размер матрицы (2 или 3): 2

Первая матрица (2x2):
Элемент [1][1]: 1
Элемент [1][2]: 2
Элемент [2][1]: 3
Элемент [2][2]: 4

Вторая матрица (2x2):
Элемент [1][1]: 2
Элемент [1][2]: 0
Элемент [2][1]: 1
Элемент [2][2]: 2

Результат умножения:
    4.00     4.00 
   10.00     8.00 
🏗️ Архитектура проекта
main.go - Главный модуль
go
package main

import (
    "fmt"
    "os"
)

func main() {
    // Вывод заголовка
    fmt.Println("=== Калькулятор матриц Кузьмин Даниил ИС-323 🧮 ===")
    fmt.Println()
    
    // Основной цикл программы
    for {
        showMenu()
        handleChoice()
    }
}

// Функции управления интерфейсом
func showMenu() { ... }
func handleChoice() { ... }
func handleMatrixAddition() { ... }
func handleScalarMultiplication() { ... }
func handleMatrixMultiplication() { ... }
func inputMatrix(size int, name string) [][]float64 { ... }
func printMatrix(matrix [][]float64) { ... }
operations.go - Математические операции
go
package main

import "errors"

// Сложение матриц
func AddMatrices(a, b [][]float64) ([][]float64, error) {
    if len(a) != len(b) || len(a[0]) != len(b[0]) {
        return nil, errors.New("матрицы должны быть одинакового размера")
    }
    
    size := len(a)
    result := make([][]float64, size)
    for i := range result {
        result[i] = make([]float64, size)
        for j := range result[i] {
            result[i][j] = a[i][j] + b[i][j]
        }
    }
    return result, nil
}

// Умножение матрицы на число
func MultiplyMatrixByScalar(matrix [][]float64, scalar float64) [][]float64 { ... }

// Умножение матриц
func MultiplyMatrices(a, b [][]float64) ([][]float64, error) { ... }
matrix.go - Расширенная структура (опционально)
go
package main

type Matrix struct {
    Rows    int
    Cols    int
    Data    [][]float64
}

// Конструктор матрицы
func NewMatrix(rows, cols int) *Matrix { ... }

// Методы матрицы
func (m *Matrix) Add(other *Matrix) (*Matrix, error) { ... }
func (m *Matrix) MultiplyScalar(scalar float64) *Matrix { ... }
func (m *Matrix) Multiply(other *Matrix) (*Matrix, error) { ... }
func (m *Matrix) Print() { ... }
🔧 Технические детали
Поддерживаемые размеры матриц
2×2 (2 строки, 2 столбца)

3×3 (3 строки, 3 столбца)

Поддерживаемые операции
Сложение матриц - поэлементное сложение

Умножение на скаляр - умножение каждого элемента на число

Умножение матриц - классическое матричное умножение

Алгоритмы реализации
Сложение матриц
go
for i := 0; i < size; i++ {
    for j := 0; j < size; j++ {
        result[i][j] = a[i][j] + b[i][j]
    }
}
Умножение матриц
go
for i := 0; i < size; i++ {
    for j := 0; j < size; j++ {
        sum := 0.0
        for k := 0; k < size; k++ {
            sum += a[i][k] * b[k][j]
        }
        result[i][j] = sum
    }
}
📊 Примеры матричных операций
Тестовые данные
bash
# Создание тестового файла
cat > test_matrices.txt << EOF
# Сложение
A = [[1, 2], [3, 4]]
B = [[5, 6], [7, 8]]
A + B = [[6, 8], [10, 12]]

# Умножение на скаляр
C = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
k = 2.5
k * C = [[2.5, 5, 7.5], [10, 12.5, 15], [17.5, 20, 22.5]]

# Умножение матриц
D = [[1, 2], [3, 4]]
E = [[2, 0], [1, 2]]
D * E = [[4, 4], [10, 8]]
EOF
Проверка корректности
bash
# Запуск автоматических тестов
go test -v ./...
🧪 Тестирование проекта
Создание тестов
bash
# Создание тестового файла
cat > operations_test.go << EOF
package main

import (
    "testing"
)

func TestAddMatrices(t *testing.T) {
    a := [][]float64{{1, 2}, {3, 4}}
    b := [][]float64{{5, 6}, {7, 8}}
    expected := [][]float64{{6, 8}, {10, 12}}
    
    result, err := AddMatrices(a, b)
    if err != nil {
        t.Fatalf("Ошибка сложения: %v", err)
    }
    
    // Проверка результата
    for i := range result {
        for j := range result[i] {
            if result[i][j] != expected[i][j] {
                t.Errorf("Неверный результат на позиции [%d][%d]", i, j)
            }
        }
    }
}

func TestMultiplyMatrixByScalar(t *testing.T) {
    matrix := [][]float64{{1, 2}, {3, 4}}
    scalar := 2.0
    expected := [][]float64{{2, 4}, {6, 8}}
    
    result := MultiplyMatrixByScalar(matrix, scalar)
    
    // Проверка результата
    for i := range result {
        for j := range result[i] {
            if result[i][j] != expected[i][j] {
                t.Errorf("Неверный результат на позиции [%d][%d]", i, j)
            }
        }
    }
}
EOF
Запуск тестов
bash
# Запуск всех тестов
go test -v

# Запуск с покрытием
go test -coverprofile=coverage.out
go tool cover -html=coverage.out
📈 Возможные улучшения
Версия 2.0 (планируемые функции)
bash
# Создание ветки для версии 2.0
git checkout -b v2.0

# Планируемые улучшения:
# 1. Поддержка матриц произвольного размера
# 2. Вычисление определителя матрицы
# 3. Транспонирование матрицы
# 4. Обращение матрицы
# 5. Решение систем линейных уравнений
# 6. Сохранение/загрузка матриц из файлов

git commit -am "feat: план улучшений для v2.0"
Версия 3.0 (долгосрочные планы)
Графический интерфейс (GUI)

Веб-интерфейс с REST API

Поддержка комплексных чисел

Собственная библиотека линейной алгебры

Интеграция с научными пакетами

🐛 Отладка и устранение ошибок
Частые ошибки
Размеры матриц не совпадают - при сложении или умножении

Некорректный ввод данных - буквы вместо чисел

Выход за границы массива - при обращении к несуществующим индексам

Решения
go
// Валидация размеров
func validateMatrixSizes(a, b [][]float64) error {
    if len(a) != len(b) || len(a[0]) != len(b[0]) {
        return fmt.Errorf(
            "размеры матриц не совпадают: %dx%d и %dx%d",
            len(a), len(a[0]), len(b), len(b[0])
        )
    }
    return nil
}

// Безопасный ввод
func safeInput(prompt string) (float64, error) {
    fmt.Print(prompt)
    var input string
    fmt.Scanln(&input)
    return strconv.ParseFloat(input, 64)
}
📝 Требования к сдаче проекта
Для ПР №3/5:
bash
# 1. Подготовить все файлы проекта
git add .
git commit -m "Подготовка к сдаче ПР №3/5"

# 2. Создать тег версии
git tag -a v1.0.0 -m "Версия для сдачи ПР №3/5"

# 3. Отправить на GitHub
git push origin main --tags

# 4. Создать архив для сдачи
git archive --format zip --output matrix-calculator-IS323-Kuzmin.zip HEAD
Структура архива для сдачи:
text
matrix-calculator-IS323-Kuzmin.zip
├── main.go
├── operations.go
├── matrix.go (опционально)
├── operations_test.go (опционально)
├── go.mod
├── go.sum
├── README.md
└── Примеры_работы.txt
🤝 Вклад в проект
Правила разработки
bash
# Создание ветки для новой функции
git checkout -b feature/имя-функции

# Регулярные коммиты
git add .
git commit -m "feat: краткое описание изменения"

# Синхронизация с основной веткой
git fetch origin
git rebase origin/main

# Создание Pull Request
git push origin feature/имя-функции
