Калькулятор матриц Кузьмин Даниил ИС-323
📌 Описание

Проект представляет собой консольное приложение на языке Go (Golang), разработанное в рамках учебной практики. Основная цель — реализация базовых операций с квадратными матрицами размером 2×2 и 3×3. Приложение обеспечивает удобный текстовый интерфейс, позволяющий пользователю выбирать нужную операцию и вводить данные вручную.

Программа написана с соблюдением принципов модульности и читаемости кода:

Каждая математическая операция вынесена в отдельную функцию.
Логика ввода, обработки и вывода разделена по соответствующим файлам.
Код легко расширяем: при необходимости можно без труда добавить поддержку других размеров матриц или новые операции (например, вычисление определителя или транспонирование).
Цель разработки:
✅ Реализовать сложение двух матриц
✅ Реализовать умножение матрицы на скаляр (вещественное число)
✅ Реализовать умножение двух матриц
✅ Обеспечить корректную обработку ошибок (например, при несовпадении размеров)
✅ Создать простой и интуитивно понятный консольный интерфейс

🧩 Структура проекта

123456
matrix-calculator/  
├── main.go          # Основной файл: запуск программы, меню, ввод/вывод, обработка выбора пользователя  
├── operations.go    # Реализация всех математических операций с матрицами  
├── matrix.go        # (опционально) Может содержать структуру Matrix и методы к ней (в текущей реализации не используется, но предусмотрен)  
├── go.mod           # Файл модуля Go, определяющий имя проекта и зависимости  
└── README.md        # Документация: описание проекта, инструкция по запу
🧾 Подробный анализ кода (по строкам и логическим блокам)

🔹 main.go

go
1
package main
Указывает, что текущий файл принадлежит основному пакету программы — точке входа.

go
1234
import (
	"fmt"
	"os"
)
Импортируются стандартные пакеты:

fmt — для форматированного ввода/вывода (печать сообщений, считывание данных).
os — для завершения программы (os.Exit(0)).
go
123
func main() {
	fmt.Println("=== Калькулятор матриц Кузьмин Даниил ИС-323 💾 ===")
	fmt.Println()
Программа запускается с приветственного заголовка, содержащего имя автора и группу — это способ идентификации работы в учебной среде.

go
123456
	fmt.Println("Выберите операцию:")
	for {
		fmt.Println("1. Сложение матриц")
		fmt.Println("2. Умножение матрицы на число")
		fmt.Println("3. Умножение двух матриц")
		fmt.Println("4. Выход")
Отображается главное меню. Используется бесконечный цикл for { }, чтобы пользователь мог выполнять несколько операций подряд без перезапуска программы.

go
12
		var choice string
		fmt.Scanln(&choice)
Программа ожидает ввода пользователя (цифры от 1 до 4). Ввод считывается как строка для упрощения обработки.

go
12345678910
		switch choice {
		case "1": handleMatrixAddition()
		case "2": handleScalarMultiplication()
		case "3": handleMatrixMultiplication()
		case "4": 
			fmt.Println("До свидания!")
			os.Exit(0)
		default:
			fmt.Println("Неверный выбор. Попробуйте снова.")
		}
Конструкция switch направляет выполнение в нужную ветку:

Сложение — вызывает handleMatrixAddition()
Умножение на число — handleScalarMultiplication()
Умножение матриц — handleMatrixMultiplication()
Выход — корректное завершение программы
Некорректный ввод — выводится предупреждение, меню повторяется
go
1234567891011121314
func handleMatrixAddition() {
	size := getMatrixSize()
	a := inputMatrix(size, "Первая матрица")
	b := inputMatrix(size, "Вторая матрица")

	result, err := AddMatrices(a, b)
	if err != nil {
		fmt.Printf("Ошибка: %s\n", err)
		return
	}

Эта функция управляет всем процессом сложения:

Запрашивает размер матрицы (2 или 3).
Последовательно получает две матрицы от пользователя.
Передаёт их в функцию AddMatrices из operations.go.
Обрабатывает возможную ошибку (например, разные размеры — хотя в текущей логике это исключено, так как размер запрашивается один раз).
Выводит результат с помощью printMatrix.
Аналогичные функции handleScalarMultiplication() и handleMatrixMultiplication() реализуют остальные операции по тому же принципу.

go
1234567891011121314
func inputMatrix(size int, name string) [][]float64 {
	fmt.Printf("\n%s (%dx%d):\n", name, size, size)
	matrix := make([][]float64, size)
	for i := range matrix {
		matrix[i] = make([]float64, size)
		for j := range matrix[i] {
			fmt.Printf("Элемент [%d][%d]: ", i+1, j+1)
			var val float64
			fmt.Scanln(&val)
			matrix[i][j] = val

Универсальная функция ввода матрицы:

Принимает желаемый размер и заголовок (например, "Вторая матрица").
Создаёт двумерный срез ([][]float64) нужного размера.
Поэлементно запрашивает значения, используя 1-индексацию для удобства пользователя ([1][1], а не [0][0]).
Возвращает заполненную матрицу.
go
123456789
func printMatrix(matrix [][]float64) {
	size := len(matrix)
	for i := 0; i < size; i++ {
		for j := 0; j < size; j++ {
			fmt.Printf("%8.2f ", matrix[i][j])
		}
		fmt.Println()
	}
}
Форматированный вывод матрицы:

Каждый элемент отображается с 2 знаками после запятой.
Занимает 8 символов по ширине, что выравнивает столбцы даже при разных значениях.
После каждой строки добавляется перевод на новую строку.
🔹 operations.go

go
123456
func AddMatrices(a, b [][]float64) ([][]float64, error) {
	if len(a) != len(b) || len(a[0]) != len(b[0]) {
		return nil, errors.New("матрицы должны быть одинакового размера")
	}
	// ... сложение поэлементно
}
Несмотря на то, что в текущем интерфейсе пользователь выбирает один размер для обеих матриц, функция всё равно содержит защиту от ошибок — это демонстрирует правильный подход к написанию надёжного кода.

go
123
func MultiplyMatrixByScalar(matrix [][]float64, scalar float64) [][]float64 {
	// Создаёт новую матрицу и умножает каждый элемент на scalar
}
Операция умножения на скаляр не может вызвать ошибку (в отличие от сложения или умножения матриц), поэтому функция не возвращает error.

go
123456
func MultiplyMatrices(a, b [][]float64) ([][]float64, error) {
	if len(a) != len(b) || len(a[0]) != len(b[0]) {
		return nil, errors.New("матрицы должны быть одинакового размера")
	}
	// Тройной цикл: i, j, k — стандартный алгоритм умножения квадратных матриц
}
Важно: в данной реализации предполагается, что обе матрицы квадратные и одного размера, что соответствует требованиям задания (только 2×2 и 3×3). Для неквадратных матриц логика потребовала бы проверки len(a[0]) == len(b), но это выходит за рамки задачи.

📝 Вывод и итоги

Проект полностью соответствует требованиям учебной практики ПР №3/5 и ПР №5:
✅ Реализованы все три обязательные операции:
 — Сложение матриц
 — Умножение матрицы на число
 — Умножение двух матриц
✅ Поддержка только квадратных матриц 2×2 и 3×3 — как и требовалось
✅ Чёткое разделение кода на модули и функции
✅ Обработка ошибок при несовпадении размеров
✅ Удобный, понятный консольный интерфейс с пошаговым вводом
✅ Читаемый, документированный и структурированный код
